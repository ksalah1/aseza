import type { Metadata } from "next";
import { setRequestLocale } from "next-intl/server";
import { Section } from "@/components/ui";
import { buildMetadata } from "@/lib/seo";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; const ar=locale==='ar'; return buildMetadata({locale,path:'/labor-visas-aqaba',title: ar?"العمالة، تصاريح العمل، والإقامة للمستثمرين في العقبة":"Labor, Work Permits, and Residency for Investors in Aqaba",description: ar?"توضيح متطلبات العمالة وتصاريح العمل والإقامة للمستثمرين في العقبة، مع التنبيه إلى أن التسجيل لا يمنح الإقامة تلقائياً.":"Overview of labor, work permit, and residency requirements for investors in Aqaba, noting registration does not automatically grant residency."});}
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; setRequestLocale(locale); const ar=locale==='ar'; return <Section width="wide"><h1 className="text-3xl font-bold text-primary">{ar?"العمالة، تصاريح العمل، والإقامة للمستثمرين في العقبة":"Labor, Work Permits, and Residency in Aqaba"}</h1><ul className="mt-4 list-disc ps-5 text-primary-600"><li>{ar?"تسجيل الشركة لا يمنح الإقامة تلقائياً":"Company registration does not automatically grant residency"}</li><li>{ar?"توظيف الأردنيين والأجانب يخضع لمتطلبات منفصلة":"Jordanian and foreign hiring follows separate compliance requirements"}</li><li>{ar?"تصاريح العمل والإقامة للمستثمر/الموظف تحتاج موافقات مستقلة":"Work permits and investor residency require separate authority approvals"}</li></ul></Section>; }
