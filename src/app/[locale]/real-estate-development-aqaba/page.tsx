import type { Metadata } from "next";
import { setRequestLocale } from "next-intl/server";
import { Section } from "@/components/ui";
import { Breadcrumb } from "@/components/ui/Breadcrumb";
import { buildMetadata } from "@/lib/seo";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; const ar=locale==='ar'; return buildMetadata({locale,path:'/real-estate-development-aqaba',title: ar?"التطوير العقاري والاستثمار في الأراضي في العقبة":"Real Estate Development and Land Investment in Aqaba",description: ar?"إرشادات للتطوير العقاري في العقبة تشمل التنظيم، رخص البناء، موافقات المشروع، وإذن الإشغال.":"Guidance for real estate development in Aqaba including zoning, building permits, development approvals, and occupancy permits."});}
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; setRequestLocale(locale); const ar=locale==='ar'; return <><div className="mx-auto max-w-7xl px-6 pt-4"><Breadcrumb items={[{ label: "الرئيسية", href: "/" }, { label: "لماذا العقبة", href: "/why-aqaba" }, { label: "التطوير العقاري" }]} /></div><Section width="wide"><h1 className="text-3xl font-bold text-primary">{ar?"التطوير العقاري والاستثمار في الأراضي في العقبة":"Real Estate Development and Land Investment in Aqaba"}</h1><ul className="mt-4 list-disc ps-5 text-primary-600"><li>{ar?"قيود التملك/التأجير واستخدامات الأراضي حسب الحالة":"Land lease/ownership caveats and land-use constraints"}</li><li>{ar?"التنظيم، رخص البناء، الموافقات التخطيطية والبيئية، وإذن الإشغال":"Zoning, building permits, planning/environment approvals, and occupancy permits"}</li><li>{ar?"تسجيل الشركة منفصل عن اعتماد المشروع العقاري":"Company registration is separate from project-level approval"}</li></ul></Section></>; }
